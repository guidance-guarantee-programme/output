<%= form_for resource, as: resource_name, url: invitation_path(resource_name),
             html: { method: :put } do |f| %>
  <div class="page-header">
    <h2><%= title t('devise.invitations.edit.header') %></h2>
  </div>

  <%= devise_error_messages! %>
  <%= f.hidden_field :invitation_token %>

  <p>
    Here are some ideas to help you create a unique, secure password:
  </p>
  <ul>
    <li>
      Choose a password with 8 characters or more and a mix of different character types.
    </li>
    <li>
      Don't use names, words found in a dictionary, phone numbers, dates, or simple combinations of
      these.
    </li>
    <li>
      Avoid using patterns of keyboard characters, such as diagonal lines of keyboard keys (eg
      njimko).
    </li>
    <li>
      Use a sequence of random characters. Include a mix of upper and lowercase letters, numbers,
      and special characters.
    </li>
  </ul>

  <div class="form-group">
    <%= f.label :password %>
    <%= f.password_field :password, class: %w(form-control input-md-6) %>
  </div>

  <div class="form-group">
    <%= f.label :password_confirmation %>
    <%= f.password_field :password_confirmation, class: %w(form-control input-md-6) %>
  </div>

  <p>
    <%= f.submit t('devise.invitations.edit.submit_button'), class: %w(t-submit) %>
  </p>
<% end %>
